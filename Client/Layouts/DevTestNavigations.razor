@namespace ToSic.Oqt.Themes.ToShineBs5.Client
@inherits Default
@using ToSic.Cre8Magic.Client.DynComponents
@using ToSic.Oqt.Themes.ToShineBs5.Client.Controls.Tests
@*
File Purpose:       Test skin to verify all kinds of navigation configurations
Recommendations:    Delete when creating your own theme. 
Nice to know:       Basically a copy of the Default.razor, but with a lot of TEST menus inside
*@
@code
{
    public override string Name => "Dev Test Navigation";
    public override string Layout => "Default";
    protected override string MagicClasses => "centered";
    protected override bool ShowSidebarNavigation => false;
    protected override bool ShowBreadcrumb => true;

    // Add some test-navigation modules to the page
    protected override void OnInitialized()
    {
        base.OnInitialized();
        DynComponents.Add(new DynComponent(PaneNames.Default, typeof(TestMenus), null));
    }

}
@{
    // Razor internal method to build with the HTML of the base Default.razor
    base.BuildRenderTree(__builder);
}