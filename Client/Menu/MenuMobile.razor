@inherits MenuMain;

@if (MenuBranch.HasChildren)
{
    <ul class="@MenuBranch.Classes("ul")">
        @foreach(var pageNav in MenuBranch.Children)
        {
            <NavItemMobile MenuBranch="pageNav"></NavItemMobile>
        }
    </ul>
}
